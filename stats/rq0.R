# have you loaded 0-load?

######## RQ0

outersect = function(x, y) {
  sort(c(setdiff(x, y),
         setdiff(y, x)))
}

nrow(projects) # total of projects in apacht
length(unique(logs$project)) # total of analyzed projects

